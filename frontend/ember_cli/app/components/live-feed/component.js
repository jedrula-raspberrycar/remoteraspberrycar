import Ember from 'ember';

export default Ember.Component.extend({
  didRender() {
    // const canvas = this.$('canvas').get(0);
    // http://jedrula.ddns.net:7002/wheels/left
    // const uri = "ws://4de93a31a4324b5dfa4e5e7881681c56.resindevice.io/video-stream";
    // const wsavc = new WSAvcPlayer(canvas, "webgl"); // , 1, 35
    // wsavc.connect(uri);
  }
});
